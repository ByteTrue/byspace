interface:
  display_name: "Sync BySpace Upstream"
  short_description: "Port a frozen upstream release delta"
  default_prompt: "Use $upstream-sync to review and port the next upstream release delta onto the current BySpace main branch."
