# @getpaseo/client

Internal BySpace workspace used by the app, CLI, server, and consolidated release package.

This package name is retained for source and protocol compatibility. It is embedded inside `@bytetrue/byspace` and is not a separately supported ByteTrue npm package or public SDK in this baseline release.
