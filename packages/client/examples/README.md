# Internal client examples

These examples exercise the repository's internal `@bytetrue/client` workspace during development. They are not a supported external SDK surface or a separately published ByteTrue package.
