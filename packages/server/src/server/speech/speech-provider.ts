/** Speech provider types shim (voice retired, issue 025 C8). */
export interface SpeechToTextProvider {
  readonly __retired: true;
}
export interface TextToSpeechProvider {
  readonly __retired: true;
}
