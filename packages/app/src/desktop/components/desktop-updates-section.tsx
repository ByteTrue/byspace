/** Local daemon section shim (Electron retired, issue 025 A3). Hidden on web. */
import type { ReactNode } from "react";

export function LocalDaemonSection(_props: Record<string, unknown>): ReactNode {
  return null;
}
