# @byspace/app

The byspace Web/PWA application, adapted from Paseo's shared Expo application.

This workspace intentionally builds only for the browser. Native platform source variants remain temporarily where shared imports require them, but byspace does not build or ship Electron, iOS, or Android clients.

```bash
npm run build:web
```
