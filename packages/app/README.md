# @bytetrue/app

BySpace web and PWA client, built with Expo (web-only).

## Development

```bash
npm run dev:app
```

Build the production static web assets:

```bash
npm run build:daemon-web-ui
```
