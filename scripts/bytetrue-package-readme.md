# BySpace

BySpace lets you monitor and control local AI coding agents from mobile, web, and the command line. It is a fork of [Paseo](https://github.com/getpaseo/paseo).

## Install the beta

```bash
npm install -g @bytetrue/byspace@beta
byspace
```

The production daemon defaults to `127.0.0.1:6777` and stores state under `~/.byspace`. Override them with `BYSPACE_LISTEN` and `BYSPACE_HOME`.

- Source: <https://github.com/ByteTrue/byspace>
- Releases: <https://github.com/ByteTrue/byspace/releases>
- Beta Web app: <https://app-beta.byspace.cc.cd>
