# Inventory API

For local development:

1. Copy `.env.example` to `.env` and choose local credentials.
2. Run `docker compose up -d db`.
3. Run `make dev`.

Run `make test` before opening a pull request. The development database is shared today; do not delete its data automatically.
