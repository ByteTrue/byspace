# Shop platform

Run `npm install`, copy `.env.example` to `.env`, migrate the local PostgreSQL database, then start `npm run dev:api` and `npm run dev:web` in separate terminals. The web app calls the API URL from `API_URL`.
